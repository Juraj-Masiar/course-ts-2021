import {moveGrid} from '../src/grid/grid';

test('move numbers right', () => {

  const inputGrid = {

  };
  const result = moveGrid(inputGrid, 'right');

  const expectedGrid = {

  };

  expect(result).toEqual(expectedGrid);
})
